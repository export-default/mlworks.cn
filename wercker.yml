box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        version: 0.13
        flags: -t hugo-redlounge -d $WERCKER_OUTPUT_DIR
deploy:
  steps:
    - duleorlovic/ftp-deploy:
        destination: $SITE
        username: $USER
        password: $PASSWORD
